import { MonitorPlay } from 'lucide-react'
import type { UtilityMeta } from '@/lib/types'

const meta: UtilityMeta = {
  id: 'thumb-tester',
  name: 'Thumbnail Tester',
  tagline: 'See your YouTube thumbnail the way viewers will — home feed, search, sidebar, phone — before you upload.',
  category: 'media',
  icon: MonitorPlay,
  hue: 5,
  keywords: ['youtube', 'thumbnail', 'preview', 'video', 'title', 'feed', 'ab test', 'compare'],
  order: 5,
}

export default meta
