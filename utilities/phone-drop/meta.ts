import { Send } from 'lucide-react'
import type { UtilityMeta } from '@/lib/types'

const meta: UtilityMeta = {
  id: 'phone-drop',
  name: 'Phone Drop',
  tagline: 'Move files, photos and text between this PC and your phone over Wi-Fi. Scan, done.',
  category: 'phone',
  icon: Send,
  hue: 165,
  keywords: ['airdrop', 'transfer', 'iphone', 'android', 'send', 'share', 'wifi', 'photos', 'clipboard', 'qr'],
  order: 20,
}

export default meta
