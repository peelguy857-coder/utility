import { Video } from 'lucide-react'
import type { UtilityMeta } from '@/lib/types'

const meta: UtilityMeta = {
  id: 'screen-recorder',
  name: 'Screen Recorder',
  tagline: 'Record the whole screen or one window straight to MP4 — game clips and tutorials without OBS.',
  category: 'media',
  icon: Video,
  hue: 350,
  keywords: ['record', 'screen', 'capture', 'video', 'mp4', 'clip', 'window', 'obs', 'gameplay'],
  order: 8,
}

export default meta
