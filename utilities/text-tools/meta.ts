import { Braces } from 'lucide-react'
import type { UtilityMeta } from '@/lib/types'

const meta: UtilityMeta = {
  id: 'text-tools',
  name: 'Text Tools',
  tagline: 'JSON, Base64, URLs, UUIDs, passwords, timestamps, case changes — the little conversions, offline.',
  category: 'dev',
  icon: Braces,
  hue: 265,
  keywords: ['json', 'format', 'base64', 'url', 'encode', 'uuid', 'password', 'timestamp', 'unix', 'case', 'hash', 'sha256'],
  order: 20,
}

export default meta
